#!/bin/bash
set -e
# On charge debconf
. /usr/share/debconf/confmodule

# 1. On interroge la base de données debconf
db_get trace-backup/client_ip || true
CLIENT_IP="$RET"

# 2. On dit au revoir à debconf
db_stop || true

# Debconf intercepte stdout dans les scripts postinst.
# On redirige stdout vers stderr pour que les messages echo soient visibles.
exec 1>&2

SHARE_DIR="/var/nfs/backupTRACE"

echo "=== Configuration du serveur NFS pour TRACE ==="

mkdir -p "$SHARE_DIR"
chown nobody:nogroup "$SHARE_DIR"
chmod 700 "$SHARE_DIR"

if [ -z "$CLIENT_IP" ]; then
    echo "ATTENTION : Aucune IP n'a été saisie. Le partage NFS est suspendu."
    exit 0
fi

touch /etc/exports

# Ajout du partage
if ! grep -q "$SHARE_DIR $CLIENT_IP" /etc/exports; then
    echo "$SHARE_DIR $CLIENT_IP(rw,sync,no_subtree_check,all_squash)" >> /etc/exports
fi

# Application de la configuration
exportfs -ra
systemctl restart nfs-kernel-server

echo "Succès : Le dossier $SHARE_DIR est partagé et sécurisé pour l'IP $CLIENT_IP."
exit 0
